import {ProtectedStackParamList} from '../../navigation';
import {RouteProp} from '@react-navigation/native';

type DetailsRouteProp = RouteProp<ProtectedStackParamList, 'Details'>;

export type {DetailsRouteProp};
