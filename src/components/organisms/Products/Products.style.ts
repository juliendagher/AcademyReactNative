import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  productContainer: {
    gap: 5,
    padding: 10,
  },
  columnWrapper: {
    justifyContent: 'space-between',
    marginBottom: 5,
  },
});

export {styles};
