import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderColor: 'gray',
    borderWidth: 1,
    borderRadius: 10,
    padding: 20,
    gap: 10,
    width: 180,
  },
  image: {
    width: 110,
    height: 110,
    borderRadius: 10,
  },
});

export {styles};
