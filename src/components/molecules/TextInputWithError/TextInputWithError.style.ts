import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    gap: 5,
  },
  input: {
    height: 44,
    borderWidth: 1,
    borderColor: 'black',
    borderRadius: 5,
    color: 'black',
  },
});

export {styles};
